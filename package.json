{
  "license": "MIT",
  "version": "0.19.4",
  "scripts": {
    "format": "prettier --write \"src/**/*.ts\"",
    "build": "bun build --format esm src/* --outdir pb_data/pb_hooks",
    "dev": "bun run build && pocketbase serve --dir=pb_data --hooksDir=pb_data/pb_hooks",
    "clean": "rm -rf pb_data"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "prettier": "^3.1.0"
  }
}
