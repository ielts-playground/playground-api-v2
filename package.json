{
  "license": "MIT",
  "version": "0.19.4",
  "scripts": {
    "format": "prettier --write \"src/**/*.ts\"",
    "build": "rm -rf pb_data/pb_hooks && bun build --format esm src/* --outdir pb_data/pb_hooks",
    "start": "pocketbase serve --dir=pb_data --hooksDir=pb_data/pb_hooks --migrationsDir=pb_migrations",
    "dev": "bun run build && bun run start",
    "reset": "rm -rf pb_data"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "prettier": "^3.1.0"
  }
}
